package dev.minevoice.neoforge.server;

import java.util.UUID;

public final class PlayerPositionSyncService {
    public void syncPosition(UUID playerId, double x, double y, double z) {
        // TODO(minevoice): publish player position updates to the voice server session layer.
    }
}
